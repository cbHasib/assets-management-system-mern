import React from "react";
import useTitle from "../../../hooks/useTitle";

const Setting = () => {
  useTitle("Setting");
  return (
    <div>
      <h2>Setting</h2>
    </div>
  );
};

export default Setting;
